# Este archivo SOLO se usa durante "mvn test" (Maven prioriza src/test/resources sobre
# src/main/resources en el classpath de test). No afecta el despliegue real en AWS.
# Evita que los tests dependan de la RDS/variables DB_* del entorno real.

spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
